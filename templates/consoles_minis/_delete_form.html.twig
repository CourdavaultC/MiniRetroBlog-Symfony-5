<form method="post" action="{{ path('consoles_minis_delete', {'id': consoles_mini.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consoles_mini.id) }}">
    <button class="btn">Delete</button>
</form>
