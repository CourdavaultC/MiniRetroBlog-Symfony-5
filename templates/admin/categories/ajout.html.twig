{% extends 'base.html.twig' %}

{% block title %}Ajouter une catégorie{% endblock %}

{% block body %}
    <h1>Ajouter une catégorie</h1>
    {{ form(form) }}
    <a class="btn btn-primary" href="{{ path("admin_categories_home") }}" role="button">Retour</a>

{% endblock %}
